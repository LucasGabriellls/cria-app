package com.test.cria.exception;

public enum ErrorCodeEnum {
    INVALID_USER_ID,
    USER_ALREADY_EXISTS,
    USER_NOT_FOUND,
    VALIDATION_FAILED,
    RESOURCE_NOT_FOUND,
    INVALID_PARAMETER_TYPE,
    MISSING_PATH_VARIABLE,
    METHOD_NOT_ALLOWED
}
